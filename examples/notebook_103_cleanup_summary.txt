Notebook 103_Running_AEP_Events_from_Atlas_14.ipynb - Exception Handling Cleanup
==================================================================================

BEFORE: 10 try / 10 except blocks (per analysis)
AFTER: 3 try / 3 except blocks (only for valid input validation)

CHANGES MADE:
=============

Cell 8 - plot_multiple_hyetographs():
--------------------------------------
REMOVED: Broad exception catch wrapping pd.read_csv()
  - except (FileNotFoundError, OSError, pd.errors.ParserError, ValueError) 
KEPT: File existence check using os.path.exists()
RATIONALE: Let pandas raise its own clear errors if CSV is invalid

Cell 43 - analyze_results():
-----------------------------
REMOVED: Broad exception catches (3 blocks total)
  1. Outer try/except around entire plan analysis
     - except (FileNotFoundError, OSError, KeyError, ValueError)
  2. Inner try/except around pipe network data
     - except (KeyError, ValueError) 
KEPT: None - all error handling removed
RATIONALE: Library functions raise clear errors; let them surface for debugging

Cell 45 - plot_results():
--------------------------
REMOVED: Broad exception catch around HDF time series plotting
  - except (FileNotFoundError, OSError, KeyError, ValueError)
KEPT: ValueError catch for parsing AEP year from plan name (line parsing)
RATIONALE: This is legitimate input parsing that may fail

REMAINING EXCEPTION HANDLING (Justified):
==========================================

Cell 8 - parse_duration():
  ✓ ValueError for invalid duration format (user input validation)

Cell 8 - read_precipitation_data():
  ✓ ValueError while parsing duration in loop (continues to next line)
  ✓ ValueError while parsing depth values (assigns NaN for invalid)

Cell 45 - plot_results():
  ✓ ValueError when parsing AEP year from plan name (continues to next)

VERIFICATION:
=============
All remaining try/except blocks are:
1. Validating user/file input format
2. Catching specific exceptions (not bare Exception)
3. Either re-raising or continuing appropriately

No broad exception handling that masks library errors.
Functions now fail fast with clear error messages.
