# Native RAS Mapper terrain export handoff

Date: 2026-08-29
Branch: `codex/native-rasmapper-terrain-export`
Pull request: not opened; explicit authorization is still required

## Delivered contract

`RasTerrain.export_rasmapper_terrain(...)` selects one registered terrain by
project/name and returns a bool-compatible `TerrainExportResult`. It exports one
nearest-neighbor Float32 GeoTIFF at exact source-derived 1x/2x/4x/8x resolution,
optionally applies native vector modifications, snaps requested bounds to an
authoritative source grid, validates raster semantics, atomically promotes the
output, and writes a JSON receipt.

The helper loads the exact `.rasmap` terrain XML, including registered source
order, priorities, masks, stitches, and modifications. An owned out-of-process
helper invokes the private HEC-RAS `TerrainLayer.GenerateNewRasTerrain`
contract verified in 6.4.1 through the locally installed 7.0.0 runtime. It does
not use Controller COM, drive a dialog, register the derivative, or reproduce
terrain-modification mathematics.

Supported exact releases are HEC-RAS 6.4.1, 6.5, and 6.6. The API checks an
explicitly supplied `RasPrj.ras_version` and its identifiable executable
release before output or native work, and it requires an explicit version to
match that exact release. HEC-RAS 6.3.x lacks the bounded contract; 6.4.0 has
an official terrain-elevation defect; 6.7 is beta-only; 7.0.0 has an official
terrain-modification export defect; and 7.0.1 was unavailable for exact
qualification. All fail closed with version-specific guidance.

## Files

- `ras_commander/terrain/RasTerrain.py`
- `ras_commander/native/RasMapperTerrainExportHelper.cs`
- `ras_commander/native/RasMapperTerrainExportHelper.exe`
- `ras_commander/native/terrain_export_host.py`
- `ras_commander/ComputeResults.py`
- `ras_commander/__init__.py`
- `ras_commander/terrain/__init__.py`
- `setup.py`
- `tests/terrain_export_host_test.py`
- `tests/qualification/terrain_export_qualification_test.py`
- `docs/api/terrain.md`
- `docs/api/index.md`
- `feature_dev_notes/Terrain_Modifications/HECRAS_63_COMPATIBILITY_2026-08-29.md`
- `feature_dev_notes/Terrain_Modifications/HECRAS_VERSION_COMPATIBILITY_2026-08-29.md`
- this feature specification and the dated research, audit, qualification, and
  handoff reports in `feature_dev_notes/Terrain_Modifications/`

## Verification summary

- Focused feature/adjacent regression suite: 105 passed after the exact-release guard.
- Ruff on changed Python/test files: passed.
- Opt-in native qualification suite on Windows: 6 passed, Wine-only case
  skipped as intended; production Wine case run separately and passed.
- Native HEC-RAS 6.4.1 and 6.5: bounded UPGU3 modification-on/off 2x and 4x
  exports and two-source stitched Muncie exports passed.
- Native HEC-RAS 6.6 UPGU3: exact 33 x 48 2x and 17 x 24 4x windows; 73 known
  channel cells changed and 1,511 control cells unchanged.
- Native HEC-RAS 6.6 Muncie: two-source stitched 16 x 23 export passed.
- Audit-only HEC-RAS 6.7 Beta 4/5 and 7.0.0 probes passed the checked windows;
  these runtimes remain rejected for the reasons above.
- Wine with HEC-RAS 6.6 runtime: production output pixel/grid/CRS-identical to
  native Windows 6.6.
- Wheel build: helper executable, helper source, and Python host included.
- Full suite: 2,393 passed, 62 skipped, 8 unrelated baseline/environment-order
  failures recorded in `QUALIFICATION_REPORT_2026-08-29.md`.

## Review focus

1. Confirm the public argument names and failure-result convention.
2. Review the exact private-method signature guard in the managed helper.
3. Review source-grid selection and the far-edge epsilon used only to neutralize
   vendor floating-point `Ceiling` behavior.
4. Review Windows Job Object and POSIX process-group ownership on timeout.
5. Review default per-call Wine-prefix cloning and the explicit already-
   task-local prefix declaration.
6. Confirm the receipt fields and semantic validation gates.

## Known gap

The dedicated CLB07 worker was unreachable. Exact HEC-RAS 6.6 Wine parity was
qualified on a controlled alternate worker with an isolated prefix. Repeating
the opt-in Wine qualification on CLB07 remains useful once that host is
available, but it is not an implementation blocker.

## Draft pull request text

Title:

> Add native modification-aware RAS Mapper terrain export

Description:

> Adds a supervised `RasTerrain.export_rasmapper_terrain()` API backed by
> HEC-RAS/RAS Mapper's registered-terrain export operation. The implementation
> preserves native source ordering, stitches, masks, and vector modifications;
> supports exact source-derived 1x/2x/4x/8x nearest-neighbor grids; validates
> the single GeoTIFF before atomic promotion; emits a typed result and JSON
> receipt; and runs on native Windows or configured Wine with owned-process
> timeout cleanup. Existing row-sampled terrain modification analysis remains
> available and unchanged.
>
> Tests cover selection, paths, grid math, multi-source compatibility,
> overwrite/receipt schemas, semantic validation, partial and timeout cleanup,
> bounded modification-on/off exports, stitched terrains, exact supported and
> unsupported version guards, audit-only intermediate-version compatibility,
> and Windows/Wine parity.
